FROM nginx:alpine

# Copy frontend files
COPY frontend/ /usr/share/nginx/html/

# Nginx config to proxy /anomalies and /chat to backend
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
